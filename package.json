{
  "name": "eva-homestead-checklist",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "db:create": "wrangler d1 create eva-homestead-checklist",
    "db:migrate:local": "wrangler d1 migrations apply eva-homestead-checklist --local",
    "db:migrate:remote": "wrangler d1 migrations apply eva-homestead-checklist --remote"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260714.1",
    "typescript": "^5.8.0",
    "wrangler": "^4.110.0"
  }
}
